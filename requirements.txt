# =============================================================================
# SynapseCortex AI | Patient 360 & Clinical Regulatory Copilot
# File    : requirements.txt
# Purpose : Python dependencies for all modules in this project
#
# Install:
#   pip install -r requirements.txt
#
# Python version: >= 3.10
# =============================================================================

# ── Data generation (data_generator/generate_synthetic_data.py) ──────────────
faker==24.0.0
pandas==2.2.2

# ── Snowflake Snowpark (app/rag_engine.py – session, SQL execution) ───────────
snowflake-snowpark-python==1.23.0

# ── Snowflake Cortex Python helpers (Complete() function) ────────────────────
# Included inside snowflake-snowpark-python >= 1.15; listed explicitly
# for clarity and to pin the version used during development.
snowflake-ml-python==1.6.4

# ── Snowflake Core SDK (Root, Cortex Search svc.search()) ────────────────────
snowflake-core==0.10.0

# ── SnowSQL / connector (standalone external connections) ────────────────────
snowflake-connector-python==3.10.1

# ── Standard utilities ────────────────────────────────────────────────────────
python-dotenv==1.0.1        # load SNOWFLAKE_* env vars from a .env file
